services:
  postgres:
    image: postgres:16
    container_name: opshub-postgres
    environment:
      POSTGRES_DB: opshub
      POSTGRES_USER: opshub
      POSTGRES_PASSWORD: opshub
    ports:
      - "5432:5432"
    volumes:
      - opshub_pgdata:/var/lib/postgresql/data

volumes:
  opshub_pgdata:
